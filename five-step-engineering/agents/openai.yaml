interface:
  display_name: "Five-Step Engineering"
  short_description: "Close engineering loops through five evidence gates"
  default_prompt: "Use $five-step-engineering to carry this change through the ordered gates, resolving blockers recursively until the outcome is verified."

policy:
  allow_implicit_invocation: false
